function gradingStudents(grades) {
    // Write your code here
    var originalGrade = grades;
    var finalGrade = [],
    for(let i = 0; i < originalGrade.length; i++) {
        
    }
    console.log(finalGrade);
    console.log(originalGrade);
}

gradingStudents([73, 67, 38, 33, 500, -2, 15]);